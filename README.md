# plex-container-auto-updater-cron
This repository provides a shell script that automatically checks for updates to your [Plex Media Server](https://www.plex.tv/) Docker container, restarts it only if an update is available, and sends a notification to a Discord channel with the version and status.
